# The E-Commerce Pipeline:
### Technologies Used:
- Supabase
- Fabric (Dataflow Gen2, M, DataLake, Warehouse)
- Power Query

 # Owl Park – Data Architecture
**Real-Time Analytics Pipeline – Zoological-like Facility**

## The Setup

### Phase 1: Ingest (The n8n Engine & Webstore)
Featuring 4 specialized ai agentic workflows running on schedules, and 
- One workflow generates dynamic pricing based on api data from <a href="https://openweathermap.org">https://openweathermap.org</a> and current sales
  - Features 2 ai agents, a price setting agent, and a specialized critic, who is instructed to make sure the price is fair
- One workflow that purchase tickets on various dates. Number of tickets and type of ticket vary depending on pricing vs weather forecast from openweathermap.org.
  - Features 2 ai agents, a shopping agent to purchase tickets, and a calendar agent to help select the date. A future improvement could be consolidating these.
- One workflow maintains stock of items sold. Purchases made to the warehouse are triggered by stock shortages and scheduled checks.
  - Features one ai agent that checks for items that are in need of replenishment, then purchases tickets from a warehouse.



### Phase 2: Store (Supabase)
Data generated by the workflows and webstore is streamed into Supabase.
* **Raw Data Repository:** Stores real-time streamed data.
* **Tables:** `Analyst_Transactions`, `Weather_Impact`, `events`, `issues`, `products`, `sales_summary`, `sellout_counter`, `transactions`, `visit_dates`, and `wh_transactions`.

### Phase 3: Transform – Model – Store (Microsoft Fabric)
A real-time pull extracts the raw data into Microsoft Fabric, where it is processed via a **Gen2 Dataflow** on a real-time schedule.

* **Data Modeling:**
    * **Dimensions (18 tables):** e.g., `dim_time`, `dim_date`, `dim_products_events`, `dim_customer_warehouse`, `dim_date_VCalendar`, etc.
    * **Facts & Bridge (8 tables):** e.g., `facts_events`, `facts_products`, `facts_transactions`, `facts_Weather_Impact`, `facts_visit_dates`, etc.
    * **Query Groups (28 Queries):** Organized into subjects: Events (4), Products (4), Sellouts (4), Transactions (3), VisitingCalendar (5), Warehouse (4), and Weather (2).
* **Storage Destinations:**
    * **Data Warehouse (Real-Time):** OLAP for structured queries.
    * **Data Lake (Every 24H):** Daily backup for historical archiving.

### Phase 4: Analyze
The modeled data is pushed live to presentation layers for analysis:
* **Power BI Reports:** Live BI Team Dashboards for Executive Analytics.
* **Claude + PBI MCP Server:** AI-Assisted Analysis using natural language.
<img width="1918" height="1031" alt="image" src="https://github.com/user-attachments/assets/c9065d5b-199c-4382-a0f8-55349bf5d36a" />
